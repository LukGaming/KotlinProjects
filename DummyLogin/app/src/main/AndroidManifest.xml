<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DummyLogin">
        <activity android:name=".features.cart.presentation.CartActivity" />
        <activity android:name=".features.homepage.presentation.HomePageActivity" />
        <activity android:name=".features.auth.presentation.LoginActivity" />
        <activity android:name=".features.user_profile.presentation.UserProfileActivity" />
        <activity android:name=".features.product_detail.presentation.ProductDetailActivity" />
        <activity
            android:name=".features.splash.presentation.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>